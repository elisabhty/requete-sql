name: Apply LEFT JOIN course update

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/codex-apply-left-join.yml"

permissions:
  contents: write

jobs:
  apply-course-patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Apply the reviewed course patch
        run: git apply .codex-left-join.patch
      - name: Commit the course and remove temporary files
        run: |
          rm .codex-left-join.patch
          rm .github/workflows/codex-apply-left-join.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Transforme LEFT JOIN en club des testeurs"
          git push origin HEAD:main
