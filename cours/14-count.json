{
  "id": 14,
  "titre": "COUNT",
  "résumé": "Compter les lignes",
  "module": {
    "titre": "Agréger",
    "sous": "Compter, calculer, regrouper"
  },
  "situation": "Tu prépares un bilan avant une campagne : tu ne veux pas la liste des clients, tu veux leur nombre total.<br><br>Avec ce que tu connais déjà, tu peux afficher leurs noms :",
  "situationSql": "SELECT nom\nFROM clients;",
  "situationTail": "Tu obtiens bien les 9 noms. Mais tu dois encore les compter à la main : pour obtenir le total directement, il faut <code>COUNT(*)</code>.",
  "studio": {
    "problem": {
      "title": "COUNT transforme plusieurs lignes en un nombre.",
      "body": "Avec <code>SELECT nom FROM clients</code>, tu obtiens les 9 lignes et tu devrais les compter toi-même. <code>COUNT(*)</code> fait ce comptage directement : SQL compte chaque ligne de la table clients et renvoie une seule valeur : 9."
    },
    "vs": {
      "bad": {
        "tag": "✕ Afficher pour compter",
        "sql": "SELECT nom FROM clients;",
        "out": "9 lignes : il faut encore les compter",
        "code": "SELECT <span class=\"vs-line bad\">nom</span>\nFROM clients;"
      },
      "good": {
        "tag": "✓ Compter avec COUNT",
        "sql": "SELECT COUNT(*) FROM clients;",
        "out": "1 ligne · une valeur : 9",
        "code": "SELECT <span class=\"vs-line good\">COUNT(*)</span>\nFROM clients;"
      }
    },
    "point": "<code>COUNT(*)</code> compte toutes les lignes. L’étoile signifie ici que le comptage porte sur les lignes, même si certaines colonnes contiennent <code>NULL</code>.",
    "why": {
      "title": "Comment COUNT arrive à 9 ?"
    },
    "viz": "count",
    "reflex": {
      "a": {
        "k": "COUNT(*)",
        "t": "compte <b>toutes</b> les lignes"
      },
      "b": {
        "k": "COUNT(colonne)",
        "t": "compte les valeurs <b>renseignées</b>"
      },
      "note": "Par exemple, <code>COUNT(email)</code> renvoie 7 car 2 clients ont un email <code>NULL</code>."
    }
  },
  "anatomie": [
    { "tok": "SELECT", "txt": "Indique ce que le résultat doit afficher." },
    { "tok": "COUNT(*)", "txt": "Compte toutes les lignes." },
    { "tok": "FROM clients", "txt": "Le comptage porte sur la table clients." }
  ],
  "exemple": "SELECT COUNT(*) FROM clients;",
  "attendu": "<b>1 ligne</b>, 1 valeur : 9. C’est le nombre total de clients.",
  "retenir": "<code>COUNT(*)</code> sert à connaître le nombre de lignes. Dans <code>SELECT COUNT(*) FROM clients</code>, les 9 clients sont comptés et le résultat affiche une seule valeur : 9. <code>COUNT(colonne)</code>, lui, ne compte que les valeurs renseignées et ignore les <code>NULL</code>.",
  "piege": {
    "t": "COUNT(*) et COUNT(colonne) ne comptent pas toujours pareil",
    "p": "Dans la table clients, <code>COUNT(*)</code> renvoie 9. Mais <code>COUNT(email)</code> renvoie 7, car 2 emails sont <code>NULL</code>."
  },
  "consigne": "Compte le nombre total de clients dans la table clients.",
  "solution": "SELECT COUNT(*) FROM clients;",
  "indice": "Tu veux compter toutes les lignes : utilise la fonction <code>COUNT</code> avec <code>*</code>.",
  "richLesson": true,
  "earlyDemo": true,
  "memo": {
    "k": "COUNT",
    "t": "Compter les lignes",
    "s": "SELECT COUNT(*) FROM table;",
    "m": "<code>COUNT(*)</code> compte toutes les lignes. <code>COUNT(colonne)</code> ignore les valeurs <code>NULL</code>."
  },
  "quiz": [
    {
      "q": "Que renvoie <code>SELECT COUNT(*) FROM clients;</code> ?",
      "o": ["Les 9 clients", "Une seule valeur : 9", "9 colonnes"],
      "b": 1,
      "e": "<code>COUNT(*)</code> compte les lignes et renvoie leur nombre."
    },
    {
      "q": "La table contient 9 clients, dont 2 avec un email <code>NULL</code>. Que renvoie <code>COUNT(email)</code> ?",
      "o": ["9", "7", "2"],
      "b": 1,
      "e": "<code>COUNT(email)</code> ignore les <code>NULL</code> : 9 - 2 = 7."
    }
  ],
  "animation": {
    "key": "count",
    "kind": "filter",
    "phases": [
      {
        "k": "SELECT nom FROM clients",
        "d": "La table clients contient 9 lignes. Afficher les noms ne donne pas directement leur nombre."
      },
      {
        "k": "SELECT COUNT(*) FROM clients",
        "d": "COUNT(*) compte les 9 lignes et les transforme en un seul résultat : 9."
      }
    ],
    "chips": [
      { "t": "Sophie", "keep": 1 },
      { "t": "Lucas", "keep": 1 },
      { "t": "Emma", "keep": 1 },
      { "t": "Nathan", "keep": 1 },
      { "t": "Chloé", "keep": 1 },
      { "t": "Hugo", "keep": 1 },
      { "t": "Léa", "keep": 1 },
      { "t": "Gabriel", "keep": 1 },
      { "t": "Inès", "keep": 1 }
    ]
  }
}
